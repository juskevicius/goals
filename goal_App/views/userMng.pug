doctype html
html(lang="en-US")
  head
    title Norian Goals
body
  form(action="/users" method="post" href="")
    label(for="name") Name:
    input(type="text" name="name")
    label(for="unitType") Unit type:
    input(type="radio" name="unitType" value="Country") Country
    input(type="radio" name="unitType" value="Department") Dep
    input(type="radio" name="unitType" value="Group" checked) Group
    label(for="name") Parent to:
    input(type="text", name="name")







//- name: {type: String, required: true, max: 25},
        owner: {type: String, required: true, max: 30},
        unitType: {type: String, required: true, enum: ['Country', 'Department', 'Group'], max: 30},
        parentTo: [{type: Schema.Types.ObjectId, ref: 'orgChart'}],
        childTo: [{type: Schema.Types.ObjectId, ref: 'orgChart'}],
    }

//- orgUnit.virtual('url').get(function() {
    return '/catalog/orgChart/' + this._id;